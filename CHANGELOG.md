# 版本记录

## 0.2.3

- 新增通用上下文选择接口：宿主提供问题和候选值，核心校验请求与返回值，不定义具体游戏的场景或指挥官规则。
- JEV 提供方以一次 choice 批处理回答多个字段；新增无状态 `/api/bridge/select-context` 网关，复用服务令牌、Origin、取消与超时机制。
- 网关不依赖额外的 TEXT 文本模型；场景落地、手动覆盖、能力/风格阈值与存档仍由宿主实现。

## 0.2.2

- 增加通用搜索接触战法：无可见敌人时按已知地图与己方访问记录探索，接触后切换计划，不读取隐藏敌情。
- 为任务增加可见单位目标绑定，移动敌人的坐标在本地更新；固定据点不受敌人移动影响。
- 按全组距离选择交战目标；远程单位进入射程后优先交火，减少无意义贴近。
- 保存每侧最近 24 条动作及回执结果、最多 512 个访问地点，兼容旧检查点。
- 模型接收具体计划步骤与己方行动历史；评分按置信度连续加权，保留有界影响与零置信度本地选择。
- 记录最近一次模型评分是否改变本地选择；独立模型输入不包含本地候选总分。

## 0.2.1

- 修复双方首次规划、目标版本、零正文窗口及取消后的扫描状态。
- 提供通用正文上下文策略及执行器响应动作扩展，不包含酒馆动作或存档规则。
- 模型调用硬上限 10，正文提取共用请求与时间预算。
- 增加无状态模型网关、核心依赖边界检查和酒馆接入文档。
- 沿用战阵非商业使用许可证 1.0；各工作区与分发源码携带许可证。

## 0.2.0

- 战术目录扩至五类、32 个模板；左右翼与区域使用参数，10 种辅助战法可组合。
- 新增可扩展能力解析、HTN 方法与操作、预测状态、回溯和执行依赖。
- 集结、火力准备、牵制、协同突击与交替掩护撤退按实际完成条件推进。
- 支持子任务修复、无进展上限、单位／资源预留，以及异步宿主命令的保存恢复。
- 缺少特殊机制时跳过可选辅助环节或选择可行方案；旧接入器兼容基础路径。
- 能力档位控制规划预算，保留全部必要步骤；模型默认只参与重新选案，每次调度最多 2 次请求。
- 调试面板支持选择战术偏好、方向和辅助战法，查看依赖状态与降级原因。
- 修复单步尚未收尾时，后续自动运行指令可能被旧暂停状态覆盖的问题。

## 0.1.1

- 修复调试面板等待请求时可能忽略暂停和手动介入的问题。
- 按保存版本丢弃晚到的旧界面响应，避免旧状态覆盖新状态。
- 增加延迟响应情况下的浏览器暂停回归测试。
- 服务启动后自动恢复未暂停的会话；关闭服务不会覆盖玩家的暂停偏好。

## 0.1.0

- 新增独立指挥核心、目标来源优先级、跨回合总计划、指挥层级及任务修订。
- 新增五档能力、十四维风格、十种战法和公开扩展接口。
- 新增静默自动运行、取消与版本检查、动作意图日志、回执去重与恢复。
- 新增官方 Jev SDK 提供方、可选正文提取、故障降级和决策回放。
- 新增两种示例地图、本机服务、中文调试界面、配置 Schema、文档和自动化测试。
- 真实酒馆集成保留至下一轮；在线模型调用尚需密钥验证。
